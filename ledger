PRAGMA database_list;
SELECT name FROM "main".sqlite_master  WHERE type IN ('table','view')   AND name NOT LIKE 'sqlite_%'   AND name LIKE ?1 ORDER BY 1;
SELECT name FROM pragma_database_list;
-- PRAGMA database_list;
SELECT sql FROM(SELECT sql, type, tbl_name, name, rowid, 1 AS snum, 'main' AS sname FROM sqlite_master) WHERE type!='meta' AND sql IS NOT NULL ORDER BY snum, rowid;
INSERT INTO ledger (type, amount, ticket_id) VALUES (2, 40000, NULL);
SELECT name FROM pragma_database_list;
-- PRAGMA database_list;
SELECT sql FROM(SELECT sql, type, tbl_name, name, rowid, 1 AS snum, 'main' AS sname FROM sqlite_master) WHERE type!='meta' AND sql IS NOT NULL ORDER BY snum, rowid;
select * from ledger;
INSERT INTO ledger (type, amount, ticket_id) VALUES (2, 10000, NULL);
INSERT INTO ledger (type, amount, ticket_id) VALUES (2, 10000, NULL);
select * from ledger;
